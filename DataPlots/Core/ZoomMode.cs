namespace DataPlots.Core
{
    public enum ZoomMode
    {
        XY,    // Zoom both X and Y axes (default)
        XOnly, // Zoom only X axis
        YOnly, // Zoom only Y axis
        None   // Disable zoom entirely
    }
}